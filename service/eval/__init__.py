"""Evaluation harness package — benchmarks the existing pipeline against
an external resume corpus. Additive: imports pipeline modules read-only,
never modifies them, and is excluded from the app import graph."""
