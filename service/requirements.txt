# The floors a human chose. NOT what the image installs — see
# requirements.lock, which is the resolved, hash-pinned answer generated
# from this file and is what the Dockerfile actually consumes.
#
# Editing this file alone changes nothing about a deploy. Regenerate the
# lock in the same commit (the exact command is in the Dockerfile, and it
# targets linux/cp311 rather than your laptop — opencv and onnxruntime ship
# platform-specific wheels, so a lock resolved on Windows or macOS produces
# hashes the image can never match).

fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
httpx>=0.27
# Server-side leg of the real-time voice interview relay
# (session/live_interview.py) to OpenAI's Realtime API.
websockets>=12.0
pypdf>=5.0
pytesseract>=0.3.10
pdf2image>=1.17
opencv-python>=4.10
numpy>=1.26
insightface>=1.0
onnxruntime>=1.18

# Sufficiency-model training pipeline (ml/). Not needed to run the face
# service; needed to regenerate assets/ml/sufficiency_model.json.
scikit-learn>=1.5
pytest>=8.0

# Encrypts Google OAuth tokens at rest (pipeline/google_oauth_store.py) —
# see GOOGLE_TOKEN_ENCRYPTION_KEY in infra/README.md.
cryptography>=42.0
