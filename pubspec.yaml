name: cognihire
description: "CogniHire — verified-claim interviewing. Continuous identity checks and adaptive follow-ups, with a transparent evidence trail instead of a hidden score."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  camera: ^0.12.0+2
  http: ^1.6.0
  meta: ^1.18.0
  camera_windows: ^0.2.6+4
  path_provider: ^2.1.6
  file_picker: ^11.0.2
  # Promoted from transitive: the session event log depends on SHA-256 directly
  # to hash-chain its entries, so the dependency must be declared, not borrowed.
  crypto: ^3.0.0
  # Backs SupabaseAuthStore (CH-1.1.2, ED-86) — the hosted AuthStore that
  # ships, replacing InMemoryAuthStore in production. Self-hostable, so it
  # fits the T-MVP single-box shape without adding a new managed dependency.
  supabase_flutter: ^2.8.0

  # Resume text extraction. All three run entirely on-device: the product's
  # promise is that a resume is never uploaded, so a cloud parsing API was never
  # an option here regardless of how much easier it would have been.
  #   syncfusion_flutter_pdf — real PDF text extraction, pure Dart.
  #   archive + xml          — a .docx is a zip whose word/document.xml holds
  #                            the text; these two read it without a converter.
  syncfusion_flutter_pdf: ^31.1.17

  # Bulk invitations: parsing HR's CSV of candidates, and sending each their
  # code by email. Real CSVs (quoted fields, embedded commas, BOM) and real
  # SMTP are both worth using a maintained library for rather than hand-rolling.
  csv: ^6.0.0
  mailer: ^6.4.1
  archive: ^4.0.7
  xml: ^6.5.0
  # Opens the browser-based recruiter registration flow (sign_in_screen.dart)
  # — platform-agnostic, unlike dart:html, so it also works under `flutter
  # test`'s VM runner rather than only in a real web build.
  url_launcher: ^6.3.2
  # Stand-in for the Kokoro streaming TTS in the target architecture — speaks
  # the OS/browser voice instead of a real spoken accent. Swap point: once a
  # Kokoro server exists, replace the `speak()` call in
  # `live_interview_screen.dart` with audio playback from that server; nothing
  # else in the turn loop changes, since the turn's `say` field is already the
  # thing being spoken either way.
  flutter_tts: ^4.2.3
  speech_to_text: ^7.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    # The sufficiency model's fitted coefficients, produced by the Python
    # trainer in service/ml/ (`python -m ml.export_model`). Checked in on
    # purpose: the app must ship a model it can score with offline, and a
    # reviewer must be able to see a weight change in a diff. The sibling
    # .report.json holding its held-out metrics is deliberately NOT bundled —
    # it is audit material for the repo, not payload for the binary.
    - assets/ml/sufficiency_model.json

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
