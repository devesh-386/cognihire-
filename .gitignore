# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Local secrets — never commit
.env
.env.*
secrets.local.json

# Generated Flutter engine artifacts. Regenerated by `flutter build`; the .pdb
# alone is ~240MB, well past GitHub's 100MB hard limit.
windows/flutter/ephemeral/
linux/flutter/ephemeral/
macos/Flutter/ephemeral/

# Python virtualenv for the face service + ML trainer. Recreate with:
#   python -m venv .venv && .venv/Scripts/pip install -r requirements.txt
service/.venv/
__pycache__/
.pytest_cache/

# Local embedding cache for training resume_fit — thousands of vectors,
# regenerable from the HF dataset, not something to version.
service/ml/resume_fit/cache/
service/ml/face_verification/cache/

# Trained resume_fit artifact — not yet wired into the app, regenerable via
# `python -m ml.resume_fit.train`. Reconsider once ai/semantic_similarity.py
# actually loads it.
service/ml/resume_fit/resume_fit_model.joblib

# Local session data. The app writes audits and face enrolments to the OS
# app-support directory, not here — but never let a stray copy be committed:
# an enrolment profile is a face embedding of a real person.
*.enrolment.json
audit_store/

# Golden-test debug output — regenerated on every failed run of
# test/preview, never an input to anything.
test/preview/failures/
.gstack/
